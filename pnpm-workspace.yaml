# pnpm reads settings here (no longer from the package.json "pnpm" field).
# Allow native install scripts needed by the Next.js build/runtime.
allowBuilds:
  sharp: true
  unrs-resolver: true
  esbuild: true
  protobufjs: true
